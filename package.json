{
  "name": "internet-transformer",
  "version": "0.1.0",
  "private": true,
  "description": "Turn any webpage into a new interactive experience on demand.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "agents": "^0.2.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
